{
  "collectionName": "components_styles_colors",
  "info": {
    "displayName": "Color"
  },
  "options": {},
  "attributes": {
    "type": {
      "type": "enumeration",
      "default": "primary",
      "enum": [
        "primary",
        "secondary",
        "tertiary",
        "white",
        "black"
      ]
    },
    "theme": {
      "type": "enumeration",
      "default": "dark",
      "enum": [
        "light",
        "dark",
        "gradient"
      ]
    },
    "hex": {
      "type": "string",
      "maxLength": 7,
      "regex": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$"
    }
  },
  "config": {}
}
